# Changelog

## 0.2.0

- 搜索增加垃圾桶入口，按需展开被自动过滤、屏蔽或当前条件隐藏的商品、原始链接和具体原因；展开不触发新请求。
- 修复 Chrome/文件导入丢失 Cookie 域名、同名跨域 Cookie 相互覆盖的问题；刷新保留来源元数据，HTTP 凭证按闲鱼域选择。
- 搜索首次读取遇到登录跳转时，等待新页面并仅重读一次；真实第二页读取验证通过。
- 工作台搜索从固定 15 条改为每页最多 30 条，增加上一页/下一页及本次已读页缓存；CLI/MCP/HTTP 支持 page 参数。
- 增加当前页的价格范围、地区、标题包含/排除筛选与数值价格排序；条件变更不重复搜索。
- 提取闲鱼价格数字旁的“万”单位，统一换算为元并保留 price_text 原文；监控和卖家信号使用相同换算。
- 网页按 Retry-After 显示搜索等待倒计时并自动重试一次，避免将本地搜索间隔只显示为“操作频繁”；状态栏增加 API 版本。
- 修复 HTTP 监控中的嵌套事件循环、批量限流跳项与失败显示为零结果。
- 增加后台任务进度与取消；Web、Android 使用短请求轮询进度。
- 统一写操作 bucket；跨线程/进程锁与原子状态写入；网络入口检查熔断。
- 告警按价格和阈值变化生成，支持已读与删除监控时清理；兼容旧数据库。
- 自动屏蔽参与过滤，信号判定先于历史和告警写入。
- 修正消息列表路由、Android 默认参数、HTTP 参数校验和 MCP 错误状态。
- 修正 Android ViewModel 工厂及布局导入，增加请求契约测试与 Debug 构建检查。
- 外部内容用文本节点渲染，限制商品链接协议；提供实际告警接口。
- 补齐 Playwright / filelock 依赖、本地诊断与运行目录；版本统一为 0.2.0。
- MCP 迁移到 2.1.1+ 的 MCPServer，支持新旧协议客户端及 stdio 输出隔离。
- CLI/MCP 增加 watch.start / job / jobs / cancel，与 PC 网页共享后台监控任务。
- 签名模块的 UTF-8 设置仅作用于 JavaScript 桥接，修复 Python 3.11 下 MCP 导入失败及其他子进程二进制输入受影响的问题。
